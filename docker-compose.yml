services:
  zeus:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - WEB_PORT=8080
      - MCP_TRANSPORT=stdio
    volumes:
      # Mount .env so secrets update without rebuild
      - ./.env:/app/.env:ro
